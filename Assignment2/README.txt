1.In order to reproduce results in part 1 "xxxx" one has to do the following:


2.In or to reproduce results in part 2, chaining, one has to do the following:
2.1 Open generating_keypoints_matches.m , choose appropriate value for Peak_Threshold (one described in report are set) , set it and run the script.
2.2 Open chaining.m, choose appropriate values for all the hyperparameters (one described in report are set), set them and run the script. Your point-view matrix will be generated and named point_view_generated. Alertnatively you can use alredy generated final_pvm.mat from now on.
